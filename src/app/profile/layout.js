import "../globals.css";

export const metadata = {
  title: "웹브릿지 대시보드",
  description: "",
};

export default function ProfileLayout({ children }) {
  return (
    <section>
        {children}
    </section>
  );
}
